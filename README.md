# Ansible Collection - cvanh.homelab

Documentation for the collection.

configure Joshua:
`ansible-playbook tasks/joshua.yml --ask-become-pass -i hosts.ini`

configure everything:
`ansible-playbook main.yml -i hosts.ini`
